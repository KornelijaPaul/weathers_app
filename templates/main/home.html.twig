{% extends 'base.html.twig' %}

{% block body %}
    <div class="card-header mb-3 bg-dark text-white text-center font-weight-bold">
        Curious what's the weather today?
    </div>
    {{ include('form/_weather.html.twig') }}
    <div id="city-tabs" class="d-none ml-5 mr-2 mt-5">
        <ul class="nav nav-pills city-list"></ul>

        <div class="info tab-content ml-5 mt-3 mt-3"></div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/weather_show.js') }}"></script>
{% endblock %}